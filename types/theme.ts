export interface Theme {
  colors: {
    background: string;
    primary: string;
    secondary: string;
    medium: string;
    light: string;
    error: string;
  };
  typography: {
    body1: object;
    headline5: object;
  };
}
