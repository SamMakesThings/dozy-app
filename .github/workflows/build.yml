name: Build

on:
  push:
    branches:
      - develop
    paths-ignore:
      - README.md
      - .eslintrc.json
      - .eslintignore
      - .gitignore
      - .prettierrc.json
  pull_request:
    branches:
      - develop
      - feature/CI
    paths-ignore:
      - README.md
      - .eslintrc.json
      - .eslintignore
      - .gitignore
      - .prettierrc.json

jobs:
  build:
    name: Build the App using EAS build
    runs-on: macos-11
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - name: Setup Expo
        uses: expo/expo-github-action@v6
        with:
          eas-version: latest
          expo-cache: true
          token: ${{ secrets.EXPO_TOKEN }}
      - name: Prepare iOS Credentials
        run: echo ${{ secrets.CREDENTIALS_JSON_BASE64 }} | base64 -d > credentials.json
      - run: mkdir ios/certs
      - name: Create iOS Distribution Certificate
        run: echo ${{ secrets.DISTRIBUTION_CERTIFICATE_BASE64 }} | base64 -d > ios/certs/distribution_certificate.p12
      - name: Create iOS Ad-Hoc Provisioning Profile
        run: echo ${{ secrets.ADHOC_PROVISION_BASE64 }} | base64 -d > ios/certs/ad_hoc_provision.mobileprovision
      - run: yarn install
      - run: eas build --platform ios --profile alpha --non-interactive
